// This file is for heroku pipeline

// web: npm start:prod   // Uncomment this for deployment

// Once the Procfile is generated , git add , git commit and git push -f heroku HEAD:master
// Then it will start building app and you will get the url of your app (deployed to heroku)

// to check logs -- heroku logs --tail
